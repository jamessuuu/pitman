{
  "name": "@pitman/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure TS: WER/alignment engine, evidence-class routing, execution-provider readback decision logic. Zero DOM dependencies — unit-testable without a browser.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
